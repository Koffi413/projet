document.getElementById("registrationForm").addEventListener("submit", function(event) {
    // Ajoutez ici le code pour traiter le formulaire avant l'envoi, si nécessaire
    // event.preventDefault(); // Utilisez cette ligne pour annuler l'envoi du formulaire par défaut
});
